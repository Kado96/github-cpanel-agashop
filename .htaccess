RewriteEngine On
RewriteBase /

# Ne pas réécrire si le fichier ou dossier existe vraiment
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, toujours renvoyer index.html (SPA)
RewriteRule ^ index.html [L]